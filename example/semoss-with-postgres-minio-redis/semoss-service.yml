apiVersion: v1
kind: Service
metadata:
  name: semoss-service
  namespace: semoss
spec:
  selector:
    app.kubernetes.io/instance: semoss
    app.kubernetes.io/name: semoss
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
